// Import local Dependencies
import React, { Component } from 'react';

// Footer Class
class Footer extends Component{
    render(){
        // Throw Content through render() Method
        return(
            <div>
                <h1>My Footer in another component</h1>
            </div>
        );
    }
}

// Mark for Attendance
export default Footer;